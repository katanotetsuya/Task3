    <div class="body">
      <div class="container">
        <% if flash[:notice] %>
        <%= flash[:notice] %>
        <% end %>
        <% if @book.errors.any? %>
          <div id="error_explanation">
            <h2><%= @book.errors.count %>errors prohibited this user from being saved:</h2>
            <ul>
            <% @book.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
      　<% end %>
        <div class="row">
          <%= render 'books/list' , user: current_user %>
          <div class="col-xs-9">
            <h2>Books</h2>
            <table class= "table">
              <thead>
                <tr>
                  <th></th>
                  <th>Title</th>
                  <th>Opinion</th>
                  <th colspan="3"></th>
                </tr>
              </thead>
              <tbody>
                <% @books.each do |books| %>
                <tr>
                  <th>
                    <%= link_to user_path(books.user) do  %>
                      <%= attachment_image_tag books.user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg" %>
                    <% end %>
                  </th>
                  <th>
                    <%= link_to book_path(books) do  %>
                      <%= books.title %>
                    <% end %>
                  </th>
                  <th>
                    <%= books.body %>
                  </th>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>